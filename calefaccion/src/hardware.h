#include <stdint.h>
#include <stdbool.h>

/**
  la funcion de ADC estara definida por el hardware final, aqui solo se
  define la cabecera para que se genere un mock
  */

  uint16_t getADC_value(void);
